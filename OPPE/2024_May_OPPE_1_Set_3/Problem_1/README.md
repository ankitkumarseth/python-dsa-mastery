# Problem 1
Implement all the given functions according to the docstrings. There will be 6 function and 6 test cases corresponding to each of the functions.

- `square_and_clip(x: int, threshold:int) -> int`
- `lowercase_first_half_and_uppercase_second_half(s: str) -> str`
- `add_the_middle_element_to_both_ends(l: list) -> None`
- `number_of_unique_common_digits(n1: int, n2: int) -> int`
- `manhattan_distance_via_b(a: tuple, b: tuple, c: tuple) -> int`
- `create_indexed_dict(names: list) -> dict`
