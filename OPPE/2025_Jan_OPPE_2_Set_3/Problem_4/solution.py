def words_with_consecutive_letters(words: list) -> list:
    """
    Takes a list of words and returns a list of words that contain at least 
    one pair of consecutive identical letters. The function is case-insensitive.
    The order of the words in the result is the same as in the input.

    Args:
        words (list): List of words.

    Returns:
        list: Words with consecutive identical letters.
    """
    pass
